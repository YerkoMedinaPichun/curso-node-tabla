# Notas:

Este es mi primer programa en Node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Numero que se multiplicará                  [number] [required]
  -l, --limit    Hasta que numero se multiplicará         [number] [default: 10]
  -s, --show     Muestra por consola la multiplicación[boolean] [default: false]
```
